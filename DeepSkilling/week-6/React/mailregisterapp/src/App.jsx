import {useState} from 'react';
function Register(){const [form,setForm]=useState({name:'',email:'',password:''}); const [errors,setErrors]=useState({}); function validate(next=form){const e={}; if(next.name.length<5)e.name='Name should have atleast 5 characters'; if(!next.email.includes('@')||!next.email.includes('.'))e.email='Email should contain @ and .'; if(next.password.length<8)e.password='Password should have atleast 8 characters'; setErrors(e); return Object.keys(e).length===0;} function change(event){const next={...form,[event.target.name]:event.target.value}; setForm(next); validate(next);} function submit(event){event.preventDefault(); if(validate()) alert('Registered successfully');} return <section className="panel"><h2>Mail Registration</h2><form onSubmit={submit}><label>Name<input name="name" value={form.name} onChange={change}/>{errors.name&&<span className="danger">{errors.name}</span>}</label><label>Email<input name="email" value={form.email} onChange={change}/>{errors.email&&<span className="danger">{errors.email}</span>}</label><label>Password<input name="password" type="password" value={form.password} onChange={change}/>{errors.password&&<span className="danger">{errors.password}</span>}</label><button>Register</button></form></section>}
export default function App(){return <main className="app"><Register/></main>}
