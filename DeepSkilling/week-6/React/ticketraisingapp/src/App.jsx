import {useState} from 'react';
function ComplaintRegister(){const [name,setName]=useState(''); const [complaint,setComplaint]=useState(''); function handleSubmit(e){e.preventDefault(); const ref=Math.floor(100000+Math.random()*900000); alert('Thanks '+name+'. Your complaint was submitted. Reference number: '+ref); setName(''); setComplaint('');} return <section className="panel"><h2>Complaint Register</h2><form onSubmit={handleSubmit}><label>Employee Name<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Complaint<textarea value={complaint} onChange={e=>setComplaint(e.target.value)} required/></label><button>Submit</button></form></section>}
export default function App(){return <main className="app"><h1>Ticket Raising App</h1><ComplaintRegister/></main>}
